{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:windows": "electron-packager . Canned --overwrite --platform=win32 --prune=true --arch=x64 --out=builds",
    "build:mac": "electron-packager . Canned --overwrite --platform=darwin --prune=true --arch=x64 --out=builds",
    "build:linux": "electron-packager . Canned --overwrite --platform=linux --prune=true --arch=x64 --out=builds"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^13.6.6",
    "electron-rebuild": "^1.10.1"
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "node-key-sender": "^1.0.11",
    "popper.js": "^1.16.1",
    "sequelize": "^5.21.5",
    "sqlite3": "^4.1.1"
  }
}
